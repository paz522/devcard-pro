name = "devcard-stripe-worker"
main = "worker.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# 環境変数は Cloudflare Dashboard で設定
# STRIPE_SECRET_KEY = "sk_..."

# 本番環境用
[env.production]
route = { pattern = "devcard-pro.workers.dev/api/checkout", custom_domain = false }
