import { Article } from "../articles";

export const tech1: Article = {
  id: "deep-technical-1",
  title: "Depths of Browser Rendering: Main Thread Optimization to Defend 60FPS and V8 Engine's JIT Strategy",
  category: "Deep Technical",
  author: "DevCard Editorial",
  date: "2026.03.11",
  readTime: "50 min",
  excerpt: "Not superficial techniques to raise Lighthouse scores, but a deep dive to achieve true UX improvement by dissecting browser internal structure. From V8 engine optimization to Compositor thread utilization.",
  blocks: [
    { type: "h2", content: "1. Dissection of Rendering Pipeline: Complete Understanding of 'The Path to Pixels'" },
    { type: "text", content: "In modern web applications, to achieve smooth animation (60FPS), the time remaining until the browser updates the screen is only 16.6ms (1000ms / 60). Controlling each stage of the process where the browser converts HTML, CSS, and JavaScript into pixels on screen, known as the 'Critical Rendering Path,' in millisecond units is the essence of frontend engineering." },
    { type: "text", content: "Rendering begins with Parsing (DOM/CSSOM construction), continues through Style (style calculation), Layout (position and size calculation), Paint (creating drawing instructions), and then Composite (layer compositing). The trap many developers fall into is that JavaScript DOM manipulation re-triggers 'Layout' or 'Paint,' occupying the main thread for extended periods. Especially, Layout (reflow) requires traversing the entire DOM tree, so it becomes exponentially heavier as the number of elements increases." },
    { type: "h3", content: "Concrete Measures to Avoid Layout Thrashing" },
    { type: "list", content: ["**Eradication of Forced Synchronous Layout (FSL)**: Reading properties like `offsetWidth` or `getComputedStyle` immediately after style changes (writing) forces the browser to execute layout to calculate the latest value. Doing this inside a loop devastates performance. The countermeasure is simple: 'Read first, write later' and execute them together.", "**Priority Use of Transform and Opacity**: Animation via `top` or `left` triggers Layout, but changes to `transform: translate()` or `opacity` are processed on the Compositor thread on the GPU without going through the main thread. This makes it possible to maintain smooth animation even when the main thread is busy with heavy JS execution.", "**Introduction of Containment Property**: Using the CSS `contain` property (`layout`, `paint`, `size`) explicitly tells the browser 'changes to this element do not affect the outside.' This allows the browser to limit recalculation scope to within that element, dramatically improving rendering performance in large DOM structures."] },
    { type: "tip", content: "Open Chrome DevTools' 'Performance' tab and look for red triangles (Long Task) or purple bars (Layout). Starting optimization without identifying bottlenecks is like shooting arrows in the dark." },
    { type: "h2", content: "2. Depths of V8 Engine: TurboFan Optimization and the 'Deoptimization' Trap" },
    { type: "text", content: "JavaScript execution speed depends on the JIT (Just-In-Time) compilation ability of the V8 engine developed by Google. V8 identifies frequently executed code (Hot Code) and compiles it into highly optimized machine code. However, depending on how developers write their code, this optimization can be解除 (Deoptimization) and fall back to slow execution by the interpreter." },
    { type: "text", content: "The key to V8 optimization is a concept called 'Hidden Classes.' Although JavaScript is a dynamic language, V8 internally tries to manage object structure statically. Objects with the same properties, in the same order, with the same type of values share the same hidden class, and property access becomes dramatically faster. Conversely, dynamically adding properties after object creation or varying the order of addition forces V8 to generate new hidden classes, and inline caching ceases to function." },
    { type: "quote", content: "For V8, predictability is speed. JavaScript that behaves like a statically typed language operates the fastest." },
    { type: "h3", content: "Guidelines for Writing JIT-Friendly Code" },
    { type: "list", content: ["**Initialize All Properties in Constructor**: Fix the object structure at the moment of instantiation. Avoid adding properties later (`obj.newProp = 1`); instead, assign `null` or `undefined` as initial values.", "**Fix Function Argument Types**: Passing different types of arguments (sometimes number, sometimes string) to one function causes V8's Crankshaft/TurboFan to judge that function as polymorphic and give up optimization. Introducing TypeScript is extremely effective not only for developers but also for engine optimization.", "**Avoidance of Sparse Arrays**: Arrays with holes like `[1, , 3]` or arrays with mixed types (Holey Elements) transition to dictionary mode inside V8, and access speed drops significantly. Always keep arrays packed."] },
    { type: "h2", content: "3. Advanced Control of Event Loop and Task Scheduling" },
    { type: "text", content: "The browser event loop manages tasks, microtasks, and rendering on a single thread. Executing heavy computation on the main thread misses rendering chances (every 16ms), causing the screen to freeze. To avoid this, techniques are needed to divide processing into small 'chunks' and return control to the browser." },
    { type: "text", content: "Especially, utilizing `requestIdleCallback` to avoid hindering user input, properly using `requestAnimationFrame` synchronized with animation, and introducing Web Workers that completely separate threads are indispensable skills in complex web applications. In the latest browsers, the `Scheduler.postTask()` API has been introduced, enabling finer control of task priority (User-visible, Background, etc.)." },
    { type: "warning", content: "Passing data to Web Workers incurs serialization costs. When transferring large data, consider optimization using `Transferable Objects` (ArrayBuffer, etc.) to move memory ownership instead of Structured Clone, reducing copy cost to zero." },
    { type: "link", content: "Evolvable Architecture Design: Trade-offs Between Performance and Maintainability", targetId: "architecture-1" }
  ]
};
